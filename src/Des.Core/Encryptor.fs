namespace Des.Core

open System
module Encryptor =
    let encrypt (plainText : string, key : string) : string =
        String.Empty

    let decrypt (cipherText : string, key : string) : string =
        String.Empty